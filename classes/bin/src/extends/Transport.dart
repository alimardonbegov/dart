class Transport {
  String typeOfTransport;
  int maxSpeed;
  int seats;
  int passangersInTrasnport;
  int maxPassengers;
  Transport(this.typeOfTransport, this.maxSpeed, this.seats,
      this.passangersInTrasnport, this.maxPassengers);

  void makeSignal() {
    print("uaaaaaaaa");
  }
}
